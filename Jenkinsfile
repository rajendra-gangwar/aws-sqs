pipeline {
    agent any

    stages {
        stage('Print Working Directory') {
            steps {
                script {
                    // Use a shell script block to execute shell commands
                    sh 'pwd'
                }
            }
        }
    }
}
